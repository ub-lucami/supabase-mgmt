version: "3.9"

services:
  mgmt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: supabase-mgmt
    networks:
      - supabase_default
    volumes:
      - /home/youruser/secrets/migration.env:/config/migration.env:ro
    tty: true

networks:
  supabase_default:
    external: true